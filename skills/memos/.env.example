# Supports either the instance root (for example http://localhost:5230)
# or the API base URL directly (for example http://localhost:5230/api/v1).
MEMOS_BASE_URL=http://localhost:5230/api/v1

# Use a long-lived personal access token when possible.
MEMOS_ACCESS_TOKEN=memos_pat_replace_me
